data:
  metadata_csv: data/metadata_augmented.csv
  image_dir: data/roofs/
  mask_dir: data/masks/
  img_size: [512, 512]
  val_split: 0.15
  num_workers: 4

model:
  # backbone: resnet34 | efficientnet-b3 | mit-b2
  encoder: efficientnet-b3
  encoder_weights: imagenet
  # auto-set by dataset, override if needed
  meta_dim: 8
  film_hidden_dim: 256
  out_channels: 3

training:
  epochs: 100
  batch_size: 8
  lr: 0.0001
  lr_schedular: cosine # cosine | step | plateau
  weight_decay: 0.0001
  panel_pixel_weight: 10
  early_stopping_patience: 20
  checkpoint_dir: checkpoint/

inference:
  threshold: 0.3
  output_dir: outputs/predictions/

logging:
  use_wandb: false
  project_name: solar-layout-designer
  run_name: baseline